# 千问AI平台 / DashScope API Key
# MCP 服务器运行时从环境变量读取 QIANWEN_API_KEY（兼容 DASHSCOPE_API_KEY / ZHIPU_API_KEY）。
# Codex 场景下请在 ~/.codex/config.toml 的 [mcp_servers.glm-vision.env] 中配置（见 README.md），
# 本文件仅作变量名参考。
QIANWEN_API_KEY=your-api-key-here

# 可选：OpenCode Zen API Key（https://opencode.ai），用于优先调用 MiMo V2.5 Free 视觉模型。
# 配置后流程变为：优先 MiMo V2.5 Free → 失败自动回退 GLM-4.6V-Flash。
# 不配置则直接使用 GLM-4.6V-Flash。
OPENCODE_API_KEY=your-opencode-key-here